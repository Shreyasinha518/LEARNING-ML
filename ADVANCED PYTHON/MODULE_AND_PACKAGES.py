from MODELS import maths,hello
print(maths.addition(12,12))
print(maths.multiplication(12,12))
# if MODELS has one more folder called models then we can import the files as:
#from MODELS.models import maths,hello